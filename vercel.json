{
  "version": 2,
  "buildCommand": "bun install && cd packages/db && bunx prisma generate && cd ../../apps/web && bun run build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "bun install",
  "framework": "nextjs",
  "regions": ["iad1"],
  "env": {
    "NODE_ENV": "production"
  }
}

